# 09 Templates

## Files
- `CalculatorClassTemplate.cpp`: Defines a generic calculator class and runs arithmetic methods for different template types.
- `FindGreatestTemplate.cpp`: Uses a function template to compare inputs and return the greatest value generically.
- `NumberClassTemplate.cpp`: Implements a number template class with operations and denominator safety check in division paths.
- `SumArrayTemplateFunction.cpp`: Uses a template function to sum array elements regardless of numeric element type.
- `SwapFunctionTemplate.cpp`: Demonstrates template-based swap working for multiple concrete data types.
- `TemplateSpecializationIncrement.cpp`: Shows generic increment behavior plus specialized handling for `char` case.

Total source files: 6
