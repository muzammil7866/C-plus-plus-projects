# Unsigned Integer Module

Behavior-based summary of files in this folder.

## Files
- `Task 2.h`
  Uses classes, inheritance.
  Executes a focused concept demo and prints runtime output based on the implemented logic.
- `Task 3.h`
  Uses classes, inheritance, operator overloading.
  Executes a focused concept demo and prints runtime output based on the implemented logic.
- `UInt16.h`
  Uses classes, operator overloading, pointers.
  Implements custom unsigned integer wrappers with overloaded arithmetic and status tracking.
- `UInt16ClassImplementation.cpp`
  Uses dynamic memory, pointers.
  Implements custom unsigned integer wrappers with overloaded arithmetic and status tracking.
- `UInt16OperatorOverloads.cpp`
  Uses operator overloading, dynamic memory, pointers, console output.
  Implements custom unsigned integer wrappers with overloaded arithmetic and status tracking.
- `UInt16TemplateCommentStub.cpp`
  Uses classes, loops, console output.
  Demonstrates generic programming with template-based functions/classes across multiple data types.
- `UInt16UsageDemo.cpp`
  Uses classes, operator overloading, loops, console output.
  Implements custom unsigned integer wrappers with overloaded arithmetic and status tracking.
- `UInt8UsageDemo.cpp`
  Uses classes, loops, console output.
  Implements custom unsigned integer wrappers with overloaded arithmetic and status tracking.
- `UIntArithmeticOperators.cpp`
  Uses dynamic memory, pointers, console output.
  Implements custom unsigned integer wrappers with overloaded arithmetic and status tracking.

Total source files: 9
